# --- Document Handling (The Core) ---
pymupdf>=1.24.0
docling>=2.8.0

# --- Intelligence (The Brain) ---
groq>=1.1.2
python-dotenv>=1.0.1
tiktoken          # Required for accurate semchunk tokenization

# --- Backend (The API) ---
fastapi>=0.115.0
uvicorn>=0.30.0

# --- Utilities ---
langdetect>=1.0.9
pydantic>=2.0.0
semchunk==4.0.0

# --- Compatibility Fixes (Crucial for NumPy conflict) ---
numpy>=1.26.4,<2.0.0
numba>=0.65.0